server {
    listen 80;
    server_name agendar.adv.br www.agendar.adv.br;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name agendar.adv.br www.agendar.adv.br;

    ssl_certificate     /etc/letsencrypt/live/agendar.adv.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/agendar.adv.br/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Upload de logos (arquivos estáticos)
    location /uploads/ {
        alias /var/www/agendaradvbr/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Proxy para o Node.js
    location / {
        proxy_pass         http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }
}
